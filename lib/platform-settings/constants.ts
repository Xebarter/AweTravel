/** Must match `012_platform_settings.sql` seed row. */
export const PLATFORM_SETTINGS_ROW_ID = 'a0000001-0000-4000-8000-000000000001';

/** Fallback when API is unavailable (5%). */
export const DEFAULT_PLATFORM_FEE_BPS = 500;
